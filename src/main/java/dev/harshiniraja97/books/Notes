BookController takes care of http request and responses. Uses a service class to Fetch all books from the database and giving it back to the API layer.
BookService- has most of the business logic. Uses the BooksRepository class and talks to the database to get the list of books and returns to the APi
BooksRepository - data access layer to the API - actually talks to the database and gets the data
